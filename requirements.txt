# CalibPrompt dependencies — pinned to the environment used for all reported results.
# PyTorch and the vendored Dassl.pytorch are installed by setup_env.sh (not here).

# NumPy < 2 is REQUIRED: torch 2.1 and Dassl's build are ABI-incompatible with numpy 2.x.
numpy==1.26.3

# Vision-language backbones (MedCLIP / BioMedCLIP / PLIP / QuiltNet use the vendored
# models/open_clip + models/medclip packages, which depend on the libraries below).
transformers==4.23.1
timm==0.6.11
huggingface_hub==0.29.1
ftfy
regex
nltk==3.7

# Calibration metrics and statistics
netcal==1.3.6
statsmodels==0.14.4
scikit-learn
scipy

# Data, config, and plotting utilities
pandas==2.2.2
pyyaml
matplotlib
tqdm
packaging
wget
